# Sobrescreve apenas o necessário para rodar dentro do docker-compose do
# repositório newelog-infra, onde o host do banco é o nome do serviço, não
# "localhost".
spring.datasource.url=jdbc:postgresql://motoristas-db:5432/motoristas_db
spring.datasource.username=${DB_USERNAME:newelog}
spring.datasource.password=${DB_PASSWORD:newelog}
